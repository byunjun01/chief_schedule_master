import streamlit as st

def show_manual():
    # 사용법 탭 — 마크다운으로 자유롭게 수정 가능합니다.
    st.markdown("""
### 📖 의국 스케줄 마스터 사용법

**작업 순서 요약**:
`① 불러오기(또는 새로) → ② 기본 설정·격주·휴진 → ③ 규칙 설정 → ④ 전공의 명단·휴가 → ⑤ 저장 → ⑥ 스케줄 배정 → ⑦ 검증 → ⑧ 엑셀 내보내기`

프로그램은 `의국스케줄마스터_실행.bat` 더블클릭으로 실행합니다 (Chrome 시크릿 창이 자동으로 열립니다 — 확장 프로그램 충돌 방지).

---
#### 📌 1. 기본 설정 (사이드바)
* **시작 월요일 / 주차 설정(4·5주)**: 스케줄 첫째 주 월요일과 주차 수. (마지막 주 수요일이 해당 달이면 포함이 원칙)
* **🔁 격주 진료 주차 설정**: 격주 진료가 이번 달 몇 주차에 들어가는지 **매달 달력에 따라 달라지므로 반드시 확인**하세요.
    * 전혜령(단독 격주): `1·3·5주` / `2·4주` 선택
    * 조우현/김계형(둘이 격주): **누가 1·3·5주인지** 선택 (한쪽이 135면 다른쪽은 자동 24). 이 선택은 그 주 담당 교수의 휴진을 정확히 반영하는 데 쓰입니다.
    * 팝업 아래 캡션에 **현재 적용 중인 설정이 항상 표시**됩니다. 백업을 불러오면 그 파일의 설정으로 바뀌니 캡션을 확인하세요.
* **공휴일**: `09-24, 09-25` 형식. **다음 달 첫 주 공휴일까지** 함께 넣어야 차리/판정 생성이 정확합니다.
* **교수 휴진 등록**: 날짜 복수 선택 + **휴진 범위(종일/오전/오후)**. 오전만 휴진이면 오후 진료는 그대로 생성됩니다. 다음 달 첫 주 휴진까지 등록 권장.
* **➕ 보충진료 추가**: 단발성 진료를 넣으면 차리/판정/참관이 자동 생성됩니다 (차리는 -2영업일).
* 여기까지 하고 **[📅 설정 및 공휴일 확정 적용]** 을 누르세요.

#### 💾 2. 저장 / 불러오기 (사이드바 하단)
* **설정 저장**: 브라우저 다운로드 또는 **[이 PC 폴더에 바로 저장]** (마지막 저장 폴더를 기억).
* **불러오기**: 파일 업로드 또는 **[이 PC 폴더에서 불러오기]** (마지막 폴더가 처음에 뜸).
* 저장 파일(JSON)에는 명단·휴가·배정 결과뿐 아니라 **격주 설정, 배율, 이동 허용, 로딩 범위, 판정 개수 범위, 하드 룰 해제, 직접 지정** 등 거의 모든 설정이 포함됩니다. 작업 중간중간 저장하세요.

#### ⚙️ 3. 규칙 설정 탭 (교수님 외래 일정)
* 교수님 외래/건증 일정이 바뀌거나 추가·제외될 때 표를 직접 수정하고 **[🔄 규칙 적용]** 을 누릅니다.
* 격주 진료는 주기 열에 `홀수주`/`짝수주`로 표시되며, 실제 몇 주차인지는 사이드바 격주 팝업에서 정합니다.

#### 👨‍⚕️ 4. 전공의 명단 탭
* 연차 선택 후 이름을 **공백/쉼표로 여러 명 한 번에 등록**할 수 있습니다.
* 역할: R3 = 의국/교육/학생/진료수석, 본원 영상(+파견 요일), 메인외래 요일 / R2 = 연건 보건소 / R1·R0 = 의국 처음.
* 수정·삭제는 카드에서 값 변경 또는 🗑 체크 후 **[💾 모든 변경사항 적용]**.
* **🚫 건증 판정참관 배정 제외 대상**: 선택된 전공의는 건증 판정은 받되 **짝 판정참관은 배정하지 않습니다** (빈 칸에는 다른 일정이 들어갈 수 있음). 검증 탭에서 별도 집계됩니다.
* **이전 달에 생성된 참관** (짝 차리/판정이 지난달인 참관) 처리:
    * `배정 안 해도 됨` = 가능하면 배정, 아니면 미배정 (선택적)
    * `아예 배정하지 않음` = 전부 미배정 처리 (이 옵션이 우선)

#### 🌴 5. 전공의 휴가 탭
* 사전휴가 / 직전휴가 / Off를 날짜 복수 선택으로 입력합니다.
* 직전휴가는 로딩 분모에서 빼지 않으며, 보건소 담당자의 직전휴가는 대체자 지정 + 처치(오후) 보충 규칙이 적용됩니다.
* **휴가가 근무일의 절반 이상인 전공의는 자동으로 '장기휴가자'** 로 처리되어 연차 내 로딩 비교에서 제외되고 별도 로딩 범위(기본 0~9, 조정 가능)만 적용됩니다.

#### 📝 6. 스케줄 배정 탭
**기본 옵션**
* **제한시간 / 배율**: 배율은 `자동` 권장. 해가 없으면 직접 1.1~1.3 지정 (배율은 로딩 **상한만** 넓힙니다).
* **깨도 되는 pairing 수 / 차리 추가 -1 이동 허용**: 해가 안 나오면 이동 허용을 늘려보세요 (건증·박진호·권혁태 묶음은 절대 안 깨짐).
* **⛔ Shift 금지 날짜**: 차리/판정이 -1일 이동해서 이 날짜에 배치되는 것을 막습니다.
* **통증클리닉 신청자**: 신청자는 통증클리닉을 최소 1개 받습니다.
* **학생 실습**: 실습으로 비워야 하는 슬롯을 직접 지정합니다.

**고급 옵션 (필요할 때만)**
* **🔧 하드 룰 임시 해제**: 20개 규칙을 체크박스로 끄고 켤 수 있습니다 (기본 전부 켜짐). 해가 없을 때 원인 규칙만 잠깐 끄는 용도.
* **📌 직접 지정 배정**: R3 전용 자리(예진·조비룡/박민선 외래참관)가 **부족한 날을 자동으로 알려주고**, 그 칸만 특정 전공의(R2 포함)로 못박아 배정합니다.
* **⚖️ 로딩 범위 / 그룹 부등호**: 그룹별 로딩 하한~상한과 그룹 간 부등호(<, <=, =)를 조정. 값을 다 고친 뒤 **[💾 적용]** 을 눌러야 반영됩니다. 그 안에:
    * **🌴 장기휴가자 로딩 범위** — 가용 절반 이하 인원 전용 범위
    * **➕ 보건소·영상 주당 추가 배정** — 보건소→처치(오후) 주1, 영상→예진 주1 (선택)
    * **📋 연차별 판정 개수 범위** — R2/R1/R0의 월 최소~최대와 주당 최대. 최소 합이 실제 판정 수를 넘으면 미리 경고합니다.

**실행**
* **[🚀 자동 스케줄 랜덤 생성]** 클릭. 결과 요약에 미배정 / **판정참관 제외** / **이전달 참관 미배정**이 구분되어 표시됩니다 (뒤의 둘은 룰에 따른 의도적 미배정 — 조치 불필요).
* 마음에 안 들면 다시 클릭 — 랜덤하게 다른 해를 보여줍니다.
* **INFEASIBLE(해 없음)** 이 뜨면: ① 이동 허용↑ ② 배율 지정 ③ 진단 실행 버튼으로 충돌 규칙 확인 ④ 하드 룰 임시 해제 순으로 시도하세요.

#### ✅ 7. 스케줄 검증 탭
* 생성 결과를 정답지와 비교해 **누락/추가/이동**을 요일별로 보여줍니다.
* 공휴일·교수 휴진(반일 포함)·규칙설정 변경·격주 설정이 사유인 누락은 자동으로 사유가 표시됩니다.
* 지표: 진짜 미배정 / 판정참관 제외 / 이전달 참관 미배정이 분리 집계됩니다.

#### 🧑‍💻 8. 확인 및 엑셀 내보내기
* **전공의 개인별 / 주차별 전체 현황** 탭에서 결과를 확인합니다.
* 주차별 전체 현황에서 **엑셀 다운로드** → 기존 의국스케줄 파일의 `N월 스케줄 - 주차별`에 붙여넣으면 크기가 맞습니다. (4주 달이면 `빈 5주차 포함` 체크로 양식 유지 가능)
* **교수별 시간표** 탭에서도 교수 시간표 엑셀을 내보낼 수 있습니다 (격주·반일 휴진 반영).

---
#### 📐 9. 배정 알고리즘 요약 (참고)
1. 생성된 모든 일정을 빠짐없이 배정 (미배정 최소화가 최우선). 단, '판정참관 제외 대상'의 참관과 '이전 달 참관' 옵션에 따른 드롭은 의도된 미배정.
2. 참관·처치·예진·조수환 건증판정(오전 고정)은 오전/오후 구분, 그 외 차리/판정은 구분 없음. 슬롯이 부족한 날의 차리/판정은 -1 영업일 이동 가능.
3. **연건 보건소**: 월~금 오전 연보 + 화/목 오후 연보 고정, 그 외 배정 없음 (주당 처치 추가 옵션 선택 시 처치(오후) 주1).
4. **본원 영상**: 주당 1개(클리닉 차리/판정 우선, 없으면 처치/예진)만 배정 (주당 예진 추가 옵션 선택 시 +1).
5. **로딩** = 배정 수 × 10 ÷ 가용 세션(휴가·공휴일 제외, 직전휴가는 분모 유지). 그룹 기본 범위:
    * 의국/교육수석 4.9~5.5 < 학생/진료수석 6.3~6.8 < 일반 R3 6.5~7.2 < R2 7.3~7.9 < 의국처음 7.5~8.0 < R1/R0 8.0~9.0
    * 같은 그룹끼리 로딩 차이 ≤ 0.3. 장기휴가자(가용 ≤ 절반)는 이 비교에서 제외.
    * 모든 범위·부등호는 배정 탭에서 조정 가능하며, 배율은 상한만 키웁니다.
6. **판정 개수**: R3 = 정확히 1개(판정=참관 같은 묶음 강제), R2 = 최소 3개, R1/R0 ≤ 주차+5, max(R2) < min(R1/R0), 의국처음 ≤ 일반 R1. 범위는 UI에서 조정 가능.
7. 차리/판정 + 참관 **pairing은 같은 사람** (건증·박진호·권혁태 묶음은 절대 분리 안 함, 그 외는 지정 개수까지 허용).
8. **조비룡/박민선 외래 차리·참관 + 예진 = R3 전용** (부족한 날은 '직접 지정 배정'으로 예외 처리).
9. 처치의 90% 이상 = R2+R3, R1 처치 ≤ 1개, 의국처음 R1·R0는 처치 제외. 처치·예진은 연차 내 ±1 균등.
10. 같은 교수 반복 배정은 최소화 (교수 중복 옵션).
    """)
